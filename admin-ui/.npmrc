# Refuse to install on a Node the project does not support (see "engines").
# Without this, npm only warns, and an old Node fails much later and far less
# legibly: vite dies on `crypto.hash is not a function`, and the test runner on
# an ERR_REQUIRE_ESM from inside a dependency. Fail here, where the message is
# about the actual problem.
engine-strict=true
